package MathXTI;
public class MathXTIClass {
	public static void main(String[] args) {
		
		// Constante de Euller e PI
		//Math.E
		//Math.PI;
		
		System.out.println(Math.sin(10)); // Seno
		System.out.println(Math.cos(10)); // Coseno
		System.out.println(Math.tan(10)); // Tangente
		System.out.println(Math.pow(4,2)); // Potência
		System.out.println(Math.sqrt(16)); // raizquadrada
		System.out.println(Math.cbrt(10)); // raizcubica
		System.out.println(Math.hypot(10,2)); // Hypotenusa
		/*
		asin= arcoseno
		acos= arcoseno
		atan = arcotangente
		exp = euler elevado a X
		sinh, cosh, tanh = seno hiperbolico
		log = logaritimo natural
		signum(x) = 1 se x for positivo, -1 for negativo, e 0 se x for igual a 0
		ceil(x) = maior número integral maior ou igual a x
		floor(x) = menor número inteiro menor ou igual a x 
		round(x) = número arredondado de x
		max(x,y) = o maior de 2 números
		min(x,y) = o menor de 2 números
		pow(x,y) = portencia de x elevado a y
		*/
		
	}
}
